spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/immaru_dev
    username: immaru_dev
    password: immaru_dev
  flyway:
    create-schemas: true
    schemas: immaru
    default-schema: immaru
immaru:
  library:
    path: /tmp/immaru/files
